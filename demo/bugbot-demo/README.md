# bugbot-demo

Tiny Node storefront used to **exercise the agent-driven Bugbot** (`pr-checker` MCP).

Baseline lives on `main`. Branch `feature/buggy-checkout` opens a PR with
**intentional** logic, security, and missing-test bugs for `/review-pr`.
